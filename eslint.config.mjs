// Minimal ESLint flat config to avoid build-time dependency on eslint-config-next
// If you want Next.js rules, install eslint-config-next and restore the extends.
export default [
  {
    rules: {},
  },
];
